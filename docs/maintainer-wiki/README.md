# Maintainer Wiki

This wiki is the canonical durable documentation layer for Carbon Memory in this repository.

Use it for architecture, workflows, protocol provenance, and maintainer decisions that should survive across sessions. Short-lived task context belongs in the local-only memory bank under `.kilocode/rules/memory-bank/`.

For repository understanding, this wiki assumes that agents will refresh [`.understand-anything/`](../../.understand-anything/) either through the preferred slash workflow [`/understand.md`](C:/Users/VSCode/.understand-anything-plugin/skills/understand/SKILL.md:7) or, if the host does not expose that alias, by directly executing the installed Understand Anything scripts and parser core.

Start with [index.md](index.md) for the full page catalog.
